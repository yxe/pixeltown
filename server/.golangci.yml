version: "2"

run:
  timeout: 2m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

    - lll
    - misspell
    - gocritic

  settings:
    lll:
      line-length: 80
      tab-width: 1
